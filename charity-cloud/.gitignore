# deps / build
node_modules/
dist/

# convex local deployment state + generated code is checked in?
# _generated IS committed here (offline tests need it; regenerated by `convex dev`)
.env.local
.env

# local convex anonymous deployment artifacts
.convex/
convex/_generated/.tmp*
